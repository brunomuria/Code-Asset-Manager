kind = "web"
previewPath = "/"
title = "SDR CRM"
version = "1.0.0"
id = "artifacts/sdr-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25774

[services.development]
run = "pnpm --filter @workspace/sdr-crm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sdr-crm", "run", "build" ]
publicDir = "artifacts/sdr-crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25774"
BASE_PATH = "/"
